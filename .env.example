# Copiar este archivo como ".env" y completar con las credenciales reales.
# El .env NO se sube al repo (está en .gitignore).

# Correo Gmail para envío de emails (IEN Empleo)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
